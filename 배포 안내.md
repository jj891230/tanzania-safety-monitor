# 탄자니아 ODA 기후·재난 안전 모니터 — 배포 안내

## 두 벌로 나뉘어 있습니다

| 파일 | 대상 | 내용 | 배포 위치 |
|---|---|---|---|
| `public/index.html` | **사업수행기관·대사관** | 31개 주 위험지도, 기후 기준선, 경보, 하천, 지진 | 인터넷 (Vercel 등) |
| `탄자니아 ODA 기후재난 안전모니터(사무소용).html` | **사무소 내부** | 위 내용 + 파견인력 명부, 연락처, Safe-Check | 로컬 / 내부망 |

**공개판에는 개인정보가 물리적으로 존재하지 않습니다.** 실행 시 숨기는 방식이 아니라
빌드 단계에서 명부·연락처·안전확인 코드를 텍스트째 잘라내며, 빌드 스크립트가 매번
잔존 여부를 검사해 하나라도 남으면 빌드를 실패시킵니다. 페이지 소스 보기로도 나오지 않습니다.

지도에서 파란 테두리의 의미가 두 판에서 다릅니다.
- 공개판 = **KOICA 사업 소재지** (공개된 정보)
- 사무소용 = **파견인력 배치 지역**

---

## Vercel 배포

### 준비
- 계정 1개 (팀 계정 권장 — 담당자 교체 시 이관 문제 방지)
- 배포 대상은 `public/` 폴더 하나뿐입니다. 빌드 과정이 없는 정적 사이트입니다.

### 방법 A — 웹에서 끌어다 놓기 (가장 간단)
1. https://vercel.com/new 접속
2. `public` 폴더를 드래그 앤 드롭
3. Deploy

### 방법 B — CLI
```bash
npm i -g vercel
cd public
vercel --prod
```

### 방법 C — Git 연동 (권장)
`public/` 을 저장소에 넣고 Vercel 프로젝트에 연결하면, 다음부터는 커밋만으로 갱신됩니다.
Root Directory 를 `public` 으로 지정하고 Framework Preset 은 `Other` 로 둡니다.

### 배포 후 확인
- 우측 상단 상태등이 초록색이고 시각이 표시되면 정상입니다.
- 회색·빨간색이면 브라우저 콘솔에서 어떤 데이터 소스가 실패했는지 확인합니다.

---

## `vercel.json` 이 하는 일

CSP(Content-Security-Policy)로 이 페이지가 접속할 수 있는 외부 주소를
**아래 4곳으로만** 제한합니다. 다른 곳으로는 어떤 요청도 나가지 않습니다.

- `api.open-meteo.com` — 기상 예보
- `flood-api.open-meteo.com` — 하천 유량
- `www.gdacs.org` — 재해 경보
- `earthquake.usgs.gov` — 지진

`form-action 'none'` 으로 어떤 폼 전송도 막고, `Referrer-Policy: no-referrer` 로
방문 기록이 외부로 새지 않게 합니다.

---

## 운영상 알아둘 것

**갱신 주기** — 페이지를 열어두면 15분마다 자동 갱신합니다. 서버가 없으므로
**아무도 페이지를 열지 않으면 아무 일도 일어나지 않습니다.** 경보를 능동적으로
받으려면 별도의 스케줄 실행(서버리스 함수 + Cron)이 필요합니다.

**API 호출량** — 방문자 브라우저가 직접 호출합니다. 한 번 열 때 4회, 이후 15분마다 4회입니다.
상시 접속자가 수십 명 수준이면 무료 한도 안에서 여유롭습니다. 그 이상으로 늘면
서버리스 함수로 한 번 받아 캐시해 나눠주는 구조로 바꾸는 것이 좋습니다.

**출처 표시 의무** — 행정경계는 geoBoundaries(CC BY 3.0 IGO)로 출처 표시가 조건입니다.
페이지 하단 각주에 이미 들어가 있으니 지우지 마십시오.

**검색 노출** — 공개판에 `noindex` 를 넣어 검색엔진에 잡히지 않습니다.
링크를 아는 사람만 접근합니다. 다만 **접근 제한은 아니므로**, 링크가 퍼지면 누구나 볼 수 있습니다.
공개해도 되는 데이터만 담은 이유가 이것입니다.

---

## 다시 빌드하려면

원본은 `원본/` 폴더의 `template.html` + 데이터 JSON 4종입니다.

```bash
cd 원본
python build.py public   ../public/index.html
python build.py internal "../탄자니아 ODA 기후재난 안전모니터(사무소용).html"
```

데이터 갱신이 필요한 경우:
- `baseline.py` — 31개 주 기후 기준선 (ERA5, 연 1회면 충분)
- `regions.py` — 행정경계 (변경 없으면 불필요)
- `rivers.py` — 하천 관측점 (변경 없으면 불필요)

---

## 1단계: 경보 자동 발송 (GitHub Actions 스케줄러)

Vercel Hobby(무료)는 자체 크론 기능이 하루 1회·±59분 오차라 재난 경보용으로 못 씁니다.
그래서 **GitHub Actions를 외부 스케줄러로 써서 15분마다 대신 호출**하게 했습니다 —
Vercel 쪽은 여전히 무료, GitHub Actions도 **공개 저장소면 완전 무료**(분당 과금 없음)입니다.

```
GitHub Actions (15분 cron)
   → curl로 /api/check-alerts 호출 (비밀키 포함)
      → Vercel Function이 실시간 데이터 수집 + 규칙 판정
         → 위험지역 있으면 이메일로 발송 (Resend API)
```

### 새로 생긴 파일
- `public/api/check-alerts.js` — 판정 로직(대시보드와 동일한 규칙을 서버에서 재실행)
- `public/data/baseline.json`, `public/data/rivers.json` — 판정에 쓰는 기준 데이터
- `.github/workflows/alert-check.yml` — 15분마다 호출하는 워크플로

로컬 스모크 테스트로 이미 검증됨(2026-09-03) — 31개 주 점검, 시미유 폭염·싱기다 강풍
2건 정상 감지, GDACS·USGS가 응답 없어도 나머지 축으로 정상 200 응답.

### 설정 순서

**① GitHub 저장소 — jj891230 계정, Public**
이 `안전모니터_배포` 폴더 전체(원본 폴더 제외)를 새 저장소로 푸시합니다.
Public이라 GitHub Actions는 15분 간격이어도 완전 무료(분당 과금 없음)입니다.
민감정보는 GitHub Actions Secrets에 있어 저장소가 공개여도 노출되지 않습니다.

**② 이메일 발송 준비 — Resend (무료)**
Vercel엔 자체 이메일 발송 기능이 없어서 무료 API가 필요합니다. Resend는
**도메인 인증 없이 바로 쓸 수 있는 발신 주소**(`onboarding@resend.dev`)를 제공해서
설정이 가장 빠릅니다(하루 100통·월 3,000통 무료 — 15분 간격으로 하루 최대 96회
점검하니 매번 경보가 나도 한도 안입니다).

1. https://resend.com → 가입(GitHub 계정으로 바로 가능)
2. 좌측 **API Keys** → **Create API Key** → 이름 아무거나 → 생성된 키 복사(한 번만 보임)
3. 도메인 인증은 **건너뛰어도 됩니다** — `onboarding@resend.dev` 발신으로 바로 발송 가능
   (나중에 자체 도메인 인증하면 발신 주소를 사무소 도메인으로 바꿀 수 있습니다)

**③ Vercel 배포**
기존과 동일하게 `public/` 을 Root Directory로 배포. 추가로 **환경변수**를
프로젝트 Settings → Environment Variables에 등록:

| 이름 | 값 |
|---|---|
| `ALERT_SECRET` | 아무 문자열이나(20자 이상 랜덤 권장) — GitHub Actions가 호출할 때 같이 보내는 비밀키 |
| `RESEND_API_KEY` | 위에서 발급받은 Resend API 키 |
| `ALERT_EMAIL_TO` | 경보 받을 이메일 주소(여러 명이면 쉼표로 구분: `a@x.com,b@y.com`) |

(`SLACK_WEBHOOK_URL`도 지원합니다 — 나중에 Slack을 쓰고 싶으면 이 환경변수만
추가로 넣으면 이메일과 동시에 발송됩니다. 코드 수정 불필요.)

**④ GitHub Actions Secrets**
저장소 Settings → Secrets and variables → Actions → New repository secret 로 2개 등록:

| 이름 | 값 |
|---|---|
| `ALERT_SECRET` | Vercel에 넣은 것과 **동일한 값** |
| `MONITOR_URL` | 배포된 주소, 끝에 슬래시 없이 (예: `https://tanzania-safety-monitor.vercel.app`) |

**⑤ 워크플로 파일이 안 들어갈 수 있습니다**
GitHub는 `.github/workflows/` 아래 파일을 푸시하려면 토큰에 **`workflow` 스코프**가
있어야 합니다. 일반 `repo` 스코프 토큰으로는 다른 파일은 다 푸시되어도 이 파일만
거부됩니다. 이 경우:
- GitHub 웹사이트에서 **직접** `.github/workflows/alert-check.yml` 파일을 만들어
  붙여넣거나(가장 확실),
- 또는 `gh auth refresh -h github.com -s workflow` 로 토큰에 스코프를 추가.

**⑥ 확인**
저장소 Actions 탭 → `탄자니아 안전모니터 - 경보 점검` → **Run workflow** 로 수동 실행.
초록색 체크가 뜨면 정상입니다. `curl -f`이 실패로 처리하는 건 엔드포인트 자체 오류
(401/502)뿐이고, "경보 없음"이나 "경보 있어서 Slack 발송함"은 둘 다 정상(200) 처리됩니다.

### 이 단계에서 일부러 안 한 것

- **중복 억제(쿨다운) 없음** — 같은 경보가 15분마다 계속 옵니다. 검증 단계 2주간은
  오히려 "얼마나 자주/일관되게 뜨는지"를 보는 게 목적이라 의도적으로 무상태로 뒀습니다.
  실사용 전환 시 Vercel Blob 등으로 마지막 상태를 저장해 "새로 발생·등급 상승"에만
  보내도록 바꿔야 합니다(2단계).
- **파견인력 개별 왓츠앱 발송 없음** — 지금은 사무소(Slack)에만 갑니다. 업체별 발송은
  WhatsApp Business API 템플릿 승인이 필요해 다음 단계입니다.

---

## 2026-09-07 변경분 — 영문 화면 · GDACS 프록시 · 경보 정책 손잡이

자세한 근거는 `검토보고서_2026-09-07_영문화_GDACS_경보정책.md`.

### 영문 화면
- 헤더 `EN` 버튼, 또는 주소 뒤에 `?lang=en` (예: `https://tanzania-safety-monitor.vercel.app/?lang=en`).
  사업수행기관에는 이 링크를 주면 된다. 선택은 브라우저에 기억된다.
- 등급 영문: Normal / Low / Moderate / High / Severe. 사업명 영문은 `원본/template.html`의 `PROJECT_EN` 표.

### GDACS
- 대시보드는 이제 `/api/gdacs`(같은 출처)만 부른다. 이 함수가 GDACS RSS를 읽어 예전 JSON 모양으로
  내려주고 CDN에 10분 캐시(만료 후 하루까지는 옛 값 제공). GDACS 서버가 느려도 화면에 "1개 소스 실패"가
  뜨는 일이 거의 없어진다. 경보 함수도 같은 방식.

### 경보 정책 환경변수 (Vercel · 전부 선택, 없으면 예전과 동일)

| 이름 | 값 | 뜻 |
|---|---|---|
| `ALERT_MODE` | `always` / `changes` | changes = 이전 점검보다 새로 생기거나 등급이 오른 곳만 즉시 메일 |
| `ALERT_MIN_LEVEL` | `2`~`4` | 즉시 메일 최소 등급(2 주의, 3 경계, 4 심각) |
| `ALERT_LEAD_DAYS` | `1`~`7` | 즉시 메일에 넣을 예보 선행일(4 = 오늘~D+3) |
| `ALERT_DIGEST_LEVEL` | `1`~`4` | 07:00 EAT 일일 요약에 넣을 최소 등급 |

권고 조합: `changes` / `3` / `4` / `2`. 값을 넣은 뒤 **재배포 + 별칭 재연결** 필수.

`changes` 모드는 저장소의 `alert-state.json`을 상태 저장소로 쓴다(워크플로가 바뀔 때만 커밋).
그러려면 저장소 Settings → Actions → General → Workflow permissions = **Read and write**,
그리고 `.github/workflows/alert-check.yml`을 GitHub 웹 에디터에 붙여넣어야 한다.
로컬에서 push 하기 전엔 `git pull --rebase`(봇 커밋과 충돌 방지).

### 워크플로 수동 실행
Actions → `탄자니아 안전모니터 - 경보 점검` → Run workflow. `digest` 체크 시 일일 요약으로 호출.

---

## 2026-09-09 변경분 — 외교부 여행경보 · 대사관 안전공지 · 대사관 연락처

- 공공데이터포털(data.go.kr) 외교부 API 3종을 서버 함수 `public/api/mofa.js`(공용 모듈 `_lib/mofa.js`)가 읽어
  대시보드 상단 패널(KPI 아래)과 푸터 연락처에 넣는다. 30분 CDN 캐시.
  - 여행경보: `TravelAlarmService2/getTravelAlarmList2` (탄자니아 = 3단계 음트와라 주 + 2단계 그 외)
  - 안전공지: `CountrySafetyService7/getCountrySafetyList7` (주탄자니아대사관 공지, 제목 클릭 시 본문)
  - 재외공관: `EmbassyService2/getEmbassyList2` (대사관 대표번호·영사콜센터)
- **환경변수 `DATA_GO_KR_KEY`**(공공데이터포털 인증키, Vercel Production에 등록됨). 키는 코드·저장소에 넣지 않는다.
  키가 없으면 패널은 "외교부 자료 불러오기 실패", 경보 메일은 여행경보 줄 없이 예전처럼 나간다.
- 경보 메일: 모든 메일 머리에 현재 여행경보 단계가 붙고, **단계가 바뀌면 기상 위험이 없어도 즉시 메일**이 간다
  (제목 "외교부 여행경보 변경 — …"). 일일 요약에는 최근 7일 대사관 공지 제목이 함께 실린다.
- 키 재발급 시: `vercel env rm DATA_GO_KR_KEY production` → `vercel env add …` → 재배포·별칭 재연결.
